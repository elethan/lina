/**
 * Legacy seed script (retired).
 *
 * This entrypoint is intentionally disabled to prevent stale demo data seeding.
 * Use the CSV-driven seeding flow instead.
 */

console.error('seed-dev.ts is retired.')
console.error('Use: npm run seed:pm-csv')
process.exit(1)
